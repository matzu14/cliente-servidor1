<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="css/bootstrap.min.css">
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to
      <%= title %>
    </p>
    <h2> Lista de zombies </h2>
    
    <Table class="table table-striped">
      <tr>
      <th>Nombre</th>
      <th>Email</th>
      <th>Tipo</th>
      </tr>
    </Table>

    <% 
      if(coleccion){
        coleccion.forEach(function(coleccion){
      %>
      <tr>
        <td> <%= zombie.name %> </td>
        <td> <%= zombie.email %> </td>
        <td> <%= zombie.type %> </td>
      </tr>

      <%
        });}
      %>
      

  
  </body>
</html>
